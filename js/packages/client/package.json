{
  "name": "client",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "rimraf dist && parcel build src/index.html",
    "release": "npm run build",
    "test": "echo 0"
  },
  "devDependencies": {
    "parcel": "^2.3.0",
    "typescript": "^4.5.5",
    "tslib": "^2.3.1",
    "rimraf": "^3.0.2"
  }
}
