{
  "name": "extension",
  "version": "0.6.0",
  "private": true,
  "targets": {
    "dev": {
      "sourceMap": {
        "inline": true,
        "inlineSources": true
      }
    },
    "prod": {
      "sourceMap": false
    }
  },
  "scripts": {
    "start": "parcel src/manifest.json --host localhost --target dev",
    "parcel-build": "rimraf dist && parcel build src/manifest.json --target prod",
    "build": "npm run parcel-build && web-ext build -n extension.zip",
    "sign": "npm run parcel-build && web-ext sign -n extension.xpi",
    "release": "npm run sign",
    "test": "jest"
  },
  "dependencies": {
    "beaverjs": "^0.3.1",
    "lit": "^2.3.1"
  },
  "devDependencies": {
    "parcel": "^2.7.0",
    "@parcel/config-webextension": "^2.7.0",
    "web-ext": "^7.2.0",
    "dotenv": "^16.0.2",
    "@types/firefox-webext-browser": "94.0.1",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.8",
    "@types/jest": "^29.0.0",
    "rimraf": "^3.0.2"
  }
}
